spring:
  application:
    name: crowd-ldap-server
  cloud:
    config:
      enabled: false
      fail-fast: true

logging:
  level:
    root: warn
    org.apache.directory: warn
    amtgroup: info
  appenders: 'CONSOLE'
